<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.virus.mystore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Navigation_Activity_Activity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".Registetion_Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NotficatiosActivity" />
        <activity android:name=".Your_Cart_Activity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Check_Out_ShoppingActivity" />
        <activity android:name=".Check_Out_PaymentActivity" />
        <activity android:name=".Check_Out_Review_Activity" />
        <activity android:name=".Invite_FriendsActivity"></activity>
    </application>

</manifest>